export default class GameField {
    // todo gamefield class

	// todo add fixed gamefield size
	// todo add borders
	// todo player cannot move outside of borders and bullets cannot move outside of borders
	// todo make everything scale with browser window size
}
